---
import Header from "@components/Header.astro";
import Layout from "@layouts/Layout.astro";
import Main from "@layouts/Main.astro";
import { getYearsForFoodType } from "@utils/navigationData";

const { foodType } = Astro.params;

// get years from static navigation data
const years = getYearsForFoodType(foodType!);
const capitalize = (str: string) => str.charAt(0).toUpperCase() + str.slice(1);
---

<Layout>
  <Header />
  <Main
    pageTitle={`Portland ${foodType ? capitalize(foodType) : "Food"} Weeks`}
  >
    <ul>
      {
        years.map(year => (
          <li>
            <wa-button href={`/${foodType}/${year}`} appearance="plain">
              <div class="wa-body-xl">{year}</div>
            </wa-button>
          </li>
        ))
      }
    </ul>
  </Main>
</Layout>
