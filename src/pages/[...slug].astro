---
// catch-all route that redirects all non-home routes to the home page
// only redirect if this is not the home page to avoid infinite redirects
if (Astro.url.pathname !== "/") {
  return Astro.redirect("/", 301);
}
---
